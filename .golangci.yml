version: "2"
run:
  allow-parallel-runners: true
linters:
  default: none
  enable:
    - copyloopvar
    - dupl
    - errcheck
    - ginkgolinter
    - goconst
    - gocyclo
    - govet
    - ineffassign
    - lll
    - misspell
    - nakedret
    - prealloc
    - revive
    - staticcheck
    - unconvert
    - unparam
    - unused
    - kubeapilinter
  settings:
    revive:
      rules:
        - name: comment-spacings
        - name: import-shadowing
    custom:
      kubeapilinter:
        type: "module"
        description: Kube API LInter lints Kube like APIs based on API conventions and best practices.
        settings:
          lintersConfig:
            conditions:
              useProtobuf: Ignore
  exclusions:
    generated: lax
    rules:
      - linters:
          - lll
        path: api/*
      - linters:
          - dupl
          - lll
        path: internal/*

        # To only run Kube API Linter on specific path
      - path-except: "core/operator/api"
        linters:
          - kubeapilinter

    paths:
      - third_party$
      - builtin$
      - examples$
formatters:
  enable:
    - gofmt
    - goimports
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
