---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: nodefileinjectors.files.barpilot.io
spec:
  group: files.barpilot.io
  names:
    kind: NodeFileInjector
    listKind: NodeFileInjectorList
    plural: nodefileinjectors
    shortNames:
    - nfi
    singular: nodefileinjector
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.path
      name: Path
      type: string
    - jsonPath: .status.nodesMatched
      name: Nodes Matched
      type: integer
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NodeFileInjector is the Schema for the nodefileinjectors API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NodeFileInjector
            properties:
              fileMode:
                default: "0644"
                description: FileMode is the file mode (permissions) in octal format
                  (e.g., "0644").
                pattern: ^0[0-7]{3}$
                type: string
              group:
                default: 0
                description: Group is the group ID that should own the file.
                format: int64
                minimum: 0
                type: integer
              jobTemplate:
                description: |-
                  JobTemplate provides optional configuration for Job-based deployments.
                  Only applicable when Mode is "job". If not specified, default values are used.
                  The template allows full customization of Job and Pod specifications including
                  resources, tolerations, affinity, security context, etc.
                properties:
                  activeDeadlineSeconds:
                    description: |-
                      ActiveDeadlineSeconds specifies the duration in seconds relative to the startTime
                      that the job may be active before the system tries to terminate it.
                    format: int64
                    type: integer
                  backoffLimit:
                    description: |-
                      BackoffLimit specifies the number of retries before marking this job failed.
                      If not set, uses the default value from Kubernetes (6).
                    format: int32
                    type: integer
                  template:
                    description: |-
                      Template describes the pod that will be created when executing a job.
                      The controller will merge this template with the required file-injector container.
                      You can use this to specify resource limits, tolerations, node affinity, etc.
                    x-kubernetes-preserve-unknown-fields: true
                  ttlSecondsAfterFinished:
                    description: |-
                      TTLSecondsAfterFinished limits the lifetime of a Job that has finished
                      execution (either Complete or Failed). If this field is set, after the Job finishes,
                      it is eligible to be automatically deleted. If this field is unset,
                      the Job won't be automatically deleted.
                    format: int32
                    type: integer
                type: object
              mode:
                allOf:
                - enum:
                  - daemonset
                  - job
                - enum:
                  - daemonset
                  - job
                default: daemonset
                description: |-
                  Mode determines how files are deployed to nodes.
                  - "daemonset": Uses a DaemonSet for continuous monitoring and updates (default)
                  - "job": Uses Jobs for one-time file injection per node
                type: string
              nodeSelector:
                additionalProperties:
                  type: string
                description: |-
                  NodeSelector selects the nodes where the file should be injected.
                  If empty, the file will be injected on all nodes.
                type: object
              owner:
                default: 0
                description: Owner is the user ID that should own the file.
                format: int64
                minimum: 0
                type: integer
              path:
                description: |-
                  Path is the absolute path on the node where the file should be written.
                  The directory will be created if it doesn't exist.
                minLength: 1
                pattern: ^/.*$
                type: string
              source:
                description: |-
                  Source defines the source of the file content (ConfigMap or Secret).
                  Exactly one of ConfigMap or Secret must be specified.
                properties:
                  configMapKeyRef:
                    description: ConfigMapKeyRef is a reference to a ConfigMapKeyRef
                      in the same namespace
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the ConfigMap or its key must
                          be defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  secretKeyRef:
                    description: SecretKeyRef is a reference to a SecretKeyRef in
                      the same namespace
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
            required:
            - path
            - source
            type: object
          status:
            description: status defines the observed state of NodeFileInjector
            properties:
              conditions:
                description: |-
                  Conditions represent the current state of the NodeFileInjector resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              daemonSetName:
                description: |-
                  DaemonSetName is the name of the managed DaemonSet.
                  Only populated when Mode is "daemonset".
                type: string
              nodeStatus:
                additionalProperties:
                  description: NodeExecutionStatus represents the status of file injection
                    on a specific node
                  properties:
                    jobName:
                      description: JobName is the name of the Job managing this node
                      type: string
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the status
                        transitioned
                      format: date-time
                      type: string
                    status:
                      description: |-
                        Status represents the current state of the Job
                        Possible values: Pending, Running, Succeeded, Failed
                      type: string
                  type: object
                description: |-
                  NodeStatus tracks the execution status of each node when Mode is "job".
                  The key is the node name, and the value contains the Job name and status.
                type: object
              nodesMatched:
                description: NodesMatched is the number of nodes that match the node
                  selector.
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
