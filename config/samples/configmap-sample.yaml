apiVersion: v1
kind: ConfigMap
metadata:
  name: my-config
  namespace: default
data:
  my-file.conf: |
    # Sample configuration file
    server {
      listen 8080;
      root /var/www/html;
    }
